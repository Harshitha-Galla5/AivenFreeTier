version: "3"

services:

  producer:
    build: ./producer
    container_name: producer
    restart: always

  processor:
    build: ./log_processor
    container_name: processor
    restart: always

  api:
    build: ./incident_service
    container_name: api
    ports:
      - "5000:5000"
    restart: always
